export type DataProps = {
    site: {
        siteMetadata: {
            title: string,
            description: string,
            websiteUrl: string,
            blogUrl: string,
            careersUrl: string,
            githubUrl: string,
            xUrl: string,
            youtubeUrl: string,
            joinUsUrl: string,
            generatedAt: Date,
        }
    }
}
